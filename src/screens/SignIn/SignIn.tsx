const SignIn: React.FunctionComponent = () => {
  return (
    <section
      data-testid="sign"
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100%",
      }}
    >
      <h1>Sign in</h1>
    </section>
  );
};

export default SignIn;
